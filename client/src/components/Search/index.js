import React, { useEffect } from "react";
import { 
  Card,
  CardContent,
  Divider,
} from "@mui/material";

const Search = () => {
  return (
    <Card elevation={0}>
      <CardContent>

      </CardContent>
      <Divider />
      <CardContent>
        
      </CardContent>
    </Card>
  );
}

export default Search;